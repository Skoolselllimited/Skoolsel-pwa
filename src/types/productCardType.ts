type Product = {
  name: string;
  price: string;
  image: string;
  campus: string;
  vendor: string;
  isSponsored?: boolean;
  isTopSeller?: boolean;
  vendorImage: string;
};

export default Product;
